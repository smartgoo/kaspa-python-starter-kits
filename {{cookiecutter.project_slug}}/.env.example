# {{ cookiecutter.project_name }} Configuration
# Copy this file to .env and customize as needed

# Kaspa Network Configuration
# Options: mainnet, testnet
KASPA_NETWORK={{ cookiecutter.kaspa_network }}

# Custom RPC endpoint (optional - auto-detected if not set)
# KASPA_RPC_URL=wss://your-rpc-endpoint

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
